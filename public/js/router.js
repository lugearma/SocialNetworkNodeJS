define(['views/index', 'views/register', 'views/login', 'views/forgootpassword'], function (IndexView, RegisterView, LoginView, ForgotPasswordView){
	var SocialRouter = Backbone.Router.extend({
		currentView = null
	});
});